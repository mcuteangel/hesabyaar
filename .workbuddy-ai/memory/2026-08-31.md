# 2026-08-31

## Plan 011 — PR #232 review triage (Phase 1, person ledger)

Triage of PR #232 review threads (CodeRabot/CodeAnt/CodeRabbit). 52 threads total:
21 live (not resolved, not outdated), 26 outdated-unresolved, 5 resolved.
Plus 3 filed issues (#234 edit-dialog recomposition, #235 move submitLoanEdit
recalculation to Rust, #236 real test coverage for migratePlaintextToEncrypted).

### Changes made this session (working tree, uncommitted)
- `RepositoryModule.kt`: DI fix. `providePersonRepository` returned
  `PersonRepositoryInterface` from a `HesabyarRepositoryInterface` param → did not
  compile. Now `provideRepository` returns concrete `HesabyarRepository`; two
  interface bindings derive from it. Verified: compileDebugKotlin BUILD SUCCESSFUL.
- `PersonBackupValidator.kt` + `rust/hesabyar-core/src/validation.rs`: added
  duplicate-person-ID rejection in BOTH validators (mirror). Added
  `normalize_person_name` in Rust so the validator derives the dedup key from
  `name` (not the backup-supplied `normalized_name`), matching the Kotlin
  `PersonNameNormalizer` already used by the parser. Rust tests pass
  (129 passed). New string `backup_validation_person_duplicate_id`.
- WIP already present (pre-existing in tree): AppDatabase chunked UPDATE (999-cap),
  BackupJsonParser/ManageBackupUseCase person PII decryption from `name`,
  RustMappers re-derive normalizedName, AGENTS.md detekt suppression note.

### Key finding — LIVE-16 detekt baseline is a FALSE POSITIVE
Reviewer said add `, ` separator to
`TooManyFunctions:HesabyarRepository.kt:HesabyarRepository : HesabyarRepositoryInterfacePersonRepositoryInterface`.
WRONG: detekt 2.0.0-alpha.6 generates the multi-interface ID with NO separator
(probe via `detektBaseline` proved it). My first edit (adding `, `) actually
BROKE the baseline. Reverted to git HEAD form; detekt now BUILD SUCCESSFUL.
Lesson: to learn the exact detekt baseline ID, point `baseline` at a temp file via
an env-var hook in app/build.gradle.kts and run `detektBaseline`, then restore
the file. Do NOT hand-edit the separator. (AGENTS.md forbids re-baselining; the
temp-probe is diagnostic only, never committed.)

### Already resolved in code (reply+resolve, no code change needed)
LIVE-1 (normalizer case-fold doc), LIVE-2 (merge preloads map), LIVE-3/4/5
(transaction backfill option (a) is intentional per D4 addendum), LIVE-6
(upsertPerson empty-key guard), LIVE-7 (both validators validate persons),
LIVE-8/9 + OUTDATED-26 (restore test asserts normalizedName/createdAt).

### Open decision (ask user before posting PR replies — external action)
Whether to reply+resolve the above as "already fixed / invalid" on GitHub.
Also: LIVE-2 batched DAO writes deferred (low value for realistic person counts).
Issues #234/#235/#236 are explicitly out-of-scope follow-ups per their own text.

### Verification status
- compileDebugKotlin: PASS
- cargo test -p hesabyar-core validation: PASS (129)
- detekt: PASS (after revert)
- Kotlin unit tests (backup/person/validator): PASS (BUILD SUCCESSFUL, 44 tasks)

### GitHub PR reply/resolve transport — 2026-08-31 afternoon
- `gh` REST works (rate_limit OK, auth mcuteangel, repo scope). `gh api graphql`
  returns persistent HTTP 502 (graphql endpoint only; REST fine). So
  `addPullRequestReviewThreadReply` + `resolveReviewThread` (GraphQL-only) fail.
- Workaround that WORKS: review-thread replies via REST
  `POST /repos/mcuteangel/hesabyaar/pulls/232/comments/{comment_id}/replies`
  with `-F "body=@file.txt"` (capital -F reads file; lowercase -f does NOT).
  Delete a bad reply via `DELETE /repos/.../pulls/comments/{id}`.
  Got all 52 comment IDs via `gh api .../pulls/232/comments?per_page=100`
  (map by path:line; first comment of a thread has in_reply_to=null).
- `resolveReviewThread` has NO REST equivalent → resolves blocked until GraphQL
  recovers OR the GitHub connector is usable.
- GitHub connector (`connector:github`, url api.githubcopilot.com/mcp) is
  ENABLED + authenticated in connectors config, but its MCP tools
  (`mcp__connector_github__*`) are NOT loaded into the current agent session.
  Needs Trust/reconnect in the WorkBuddy connector panel (+ maybe session reload)
  before its tools appear. Could not invoke it.
- Posted 1 reply so far (detekt false-positive thread, REST): comment id
  3893036316. Bad duplicate (3893033051) deleted.
